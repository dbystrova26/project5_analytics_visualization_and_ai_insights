# LangChain stack — install via pip
langchain>=0.3
langgraph>=0.2
langchain-anthropic>=0.2
langchain-openai>=0.2
langsmith>=0.1
langchain-core>=0.3
langchain-text-splitters>=0.2
python-dotenv>=1.0
anthropic>=0.20
openai>=1.0
tiktoken>=0.7
httpx>=0.25

# Scientific stack — install via conda (recommended on Windows)
# conda install pandas numpy scipy -y
# If not using conda, pip will install these too but may require C++ build tools
pandas>=2.2
numpy>=2.0
scipy>=1.11

# Data download
kaggle>=1.6
